from setuptools import find_packages, setup

setup(
    name='netbox_snmp',
    version='0.0.0',
    description=' NetBox snmp community plugin',
    install_requires=[],
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)
